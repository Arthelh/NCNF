<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ncnf" >

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bootcamp" >
        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />

        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name="ncnf.feed.FeedActivity"
            android:label="@string/title_activity_feed"
            android:parentActivityName="ncnf.main.MainActivity" >
        </activity>

        <activity
            android:name="ncnf.main.DisplayMessageActivity"
            android:parentActivityName="ncnf.main.MainActivity" >
        </activity>

        <activity
            android:name="ncnf.feed.EventActivity"
            android:parentActivityName="ncnf.feed.FeedActivity">
        </activity>

        <activity android:name="ncnf.main.MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>